# Удалённые компоненты в документации

Дата: 2025-11-25  
Версия проекта: 1.0.2  
Методология: Repository Planning Graph (RPG)

---

## Резюме

В ходе аудита документации обнаружены упоминания компонентов, которые были удалены в процессе миграции на core-only архитектуру (версии 0.9.0 - 1.0.0).

**Всего найдено**: **11 упоминаний** в **5 файлах**

### Распределение по типам компонентов
- `py_accountant.sdk.*` — 3 упоминания (контекстные, не блокирующие)
- `presentation.cli.*` — 6 упоминаний (критические блокеры)
- ApplicationService — 0 упоминаний (отсутствует)
- Sync репозитории (SqlAlchemyRepository/UnitOfWork) — 0 упоминаний (отсутствует)
- Старые импорты (`application.interfaces`) — 0 упоминаний (отсутствует)

---

## 1. py_accountant.sdk.*

### Контекст: SDK был удалён в версии 0.9.0 (CORE-01)

Все найденные упоминания являются **контекстными** — они объясняют историю миграции и предупреждают о возможной путанице. **Не требуют удаления**, но могут быть уточнены.

#### README.md

- [ ] **Строка 13**: Контекстное объяснение удаления SDK
  ```markdown
  В репозитории хранится "core-only" реализация (Domain / Application / Infrastructure). 
  В прошлом в пакете публиковался отдельный SDK-слой (`py_accountant.sdk.*`).
  ```
  **Статус**: ✅ Корректно (объясняет миграцию)  
  **Действие**: Оставить как есть

- [ ] **Строка 15**: Предупреждение о site-packages
  ```markdown
  Если вы видите в traceback импорт из `.../site-packages/py_accountant/...` 
  или в IDE — External Libraries — присутствует `py_accountant.sdk`
  ```
  **Статус**: ✅ Корректно (важное предупреждение)  
  **Действие**: Оставить как есть

- [ ] **Строка 163**: Повторное упоминание
  ```markdown
  для окружения, но никаких модулей `py_accountant.sdk.*` больше нет.
  ```
  **Статус**: ✅ Корректно (подтверждает удаление)  
  **Действие**: Оставить как есть

#### docs/DOCUMENTATION_FIX_PROPOSAL.md

- [ ] **Строка 851**: Пример миграции импортов (в таблице сравнения)
  ```markdown
  `from py_accountant.sdk.accounts import CreateAccount`  
  `from py_accountant.application.use_cases_async.accounts import AsyncCreateAccount`
  ```
  **Статус**: ✅ Корректно (документирует миграцию)  
  **Действие**: Оставить как есть

- [ ] **Строка 928**: Пример sed-команды для автоматической замены
  ```bash
  find . -name "*.py" -exec sed -i '' 's/from py_accountant.sdk./from py_accountant.application.use_cases_async./g' {} \;
  ```
  **Статус**: ✅ Корректно (инструкция по миграции)  
  **Действие**: Оставить как есть

**Вывод по SDK**: Все 5 упоминаний носят **образовательный характер** и помогают пользователям понять миграцию. Удаление не требуется.

---

## 2. Presentation Layer (presentation.cli.*)

### Контекст: Presentation layer был удалён в версии 1.0.0

Найдено **6 упоминаний** команд CLI, которые ссылаются на несуществующий модуль `presentation.cli.main`. Это **критические блокеры** — пользователи не смогут выполнить описанные команды.

#### docs/FX_AUDIT.md

- [ ] **Строка 53**: Описание CLI-группы `fx`
  ```markdown
  CLI-группа: `fx` (см. `src/presentation/cli/fx_audit.py`). 
  Все команды async, JSON-вывод включается флагом `--json`.
  ```
  **Статус**: 🔴 Блокер  
  **Проблема**: Модуль `src/presentation/cli/fx_audit.py` не существует  
  **Действие**: Заменить на документацию использования через use cases API

- [ ] **Строка 59**: Пример команды add-event
  ```bash
  poetry run python -m presentation.cli.main fx add-event CODE RATE [--occurred-at ISO] [--source TEXT] [--json]
  ```
  **Статус**: 🔴 Блокер  
  **Проблема**: Команда не работает  
  **Действие**: Показать использование через `AsyncAddExchangeRateEvent` use case

#### docs/RUNNING_MIGRATIONS.md

- [ ] **Строка 88**: Пример команды trading detailed
  ```bash
  poetry run python -m presentation.cli.main trading detailed --base USD
  ```
  **Статус**: 🔴 Блокер  
  **Проблема**: Команда не работает  
  **Действие**: Показать использование через `AsyncGetTradingBalance` use case

#### docs/TRADING_WINDOWS.md

- [ ] **Строка 56**: Пример команды trading raw
  ```bash
  poetry run python -m presentation.cli.main trading raw --json
  ```
  **Статус**: 🔴 Блокер  
  **Проблема**: Команда не работает  
  **Действие**: Показать использование через API

- [ ] **Строка 60**: Повторный пример с параметрами
  ```bash
  poetry run python -m presentation.cli.main trading raw \
    --account "Assets:Cash" --account "Liabilities:Cards" --json
  ```
  **Статус**: 🔴 Блокер  
  **Проблема**: Команда не работает  
  **Действие**: Показать использование через API

#### docs/ARCHITECTURE_OVERVIEW.md

- [ ] **Строка 134**: Упоминание Planned CLI/Presentation
  ```markdown
  (Planned) CLI/Presentation: `src/presentation/cli/main.py`, 
  `src/presentation/cli/*.py` — этот слой описан в архитектуре 
  как возможное будущее HTTP/CLI API и может отсутствовать 
  в текущей версии репозитория
  ```
  **Статус**: 🟡 Вводит в заблуждение  
  **Проблема**: Presentation layer был удалён (не "planned", а "removed")  
  **Действие**: Убрать упоминание или указать, что слой удалён

**Вывод по Presentation**: Все 6 упоминаний являются **критическими** и должны быть исправлены в спринте S2.

---

## 3. ApplicationService

### Поиск: `grep -rn "ApplicationService" docs/ README.md`

**Результат**: Упоминаний не найдено ✅

ApplicationService никогда не был задокументирован или был удалён ранее в процессе предыдущих правок.

---

## 4. Sync репозитории (SqlAlchemyRepository, SqlAlchemyUnitOfWork)

### Контекст: Sync API был удалён в версиях 0.9.0-1.0.0

### Поиск: `grep -rn "SqlAlchemyRepository\|SqlAlchemyUnitOfWork" docs/ README.md`

**Результат**: Упоминаний не найдено ✅

Sync репозитории корректно удалены из документации.

---

## 5. Старые импорты (application.interfaces)

### Контекст: Модуль application.interfaces был переименован в application.ports

### Поиск: `grep -rn "from py_accountant.application.interfaces" docs/ README.md`

**Результат**: Упоминаний не найдено ✅

Старые импорты корректно обновлены в документации.

---

## Сводная таблица проблем

| Файл | Компонент | Количество упоминаний | Приоритет | Спринт |
|------|-----------|----------------------|-----------|--------|
| README.md | py_accountant.sdk.* | 3 | ✅ OK (контекст) | - |
| docs/DOCUMENTATION_FIX_PROPOSAL.md | py_accountant.sdk.* | 2 | ✅ OK (контекст) | - |
| docs/FX_AUDIT.md | presentation.cli | 2 | 🔴 P0 | S2 |
| docs/RUNNING_MIGRATIONS.md | presentation.cli | 1 | 🔴 P0 | S2 |
| docs/TRADING_WINDOWS.md | presentation.cli | 2 | 🔴 P0 | S2 |
| docs/ARCHITECTURE_OVERVIEW.md | presentation.cli | 1 | 🟡 P1 | S2 |

---

## Приоритеты исправлений

### P0 - Критические (блокируют использование)

**5 упоминаний в 3 файлах:**
1. docs/FX_AUDIT.md — 2 упоминания (строки 53, 59)
2. docs/RUNNING_MIGRATIONS.md — 1 упоминание (строка 88)
3. docs/TRADING_WINDOWS.md — 2 упоминания (строки 56, 60)

**Действие**: Заменить примеры CLI-команд на использование через Python API (async use cases)

### P1 - Высокие (вводят в заблуждение)

**1 упоминание в 1 файле:**
1. docs/ARCHITECTURE_OVERVIEW.md — 1 упоминание (строка 134)

**Действие**: Убрать упоминание presentation layer из архитектурного описания

### ✅ Не требуют исправления

**5 упоминаний SDK в 2 файлах** носят образовательный характер и помогают понять миграцию.

---

## Детальные рекомендации по исправлению

### Для docs/FX_AUDIT.md

**Заменить** (строки 53-60):
```markdown
CLI-группа: `fx` (см. `src/presentation/cli/fx_audit.py`). 
Все команды async, JSON-вывод включается флагом `--json`.

Примеры:
- `poetry run python -m presentation.cli.main fx add-event CODE RATE [--occurred-at ISO] [--source TEXT] [--json]`
```

**На**:
```markdown
## Использование через Python API

FX audit функции доступны через async use cases:

```python
from py_accountant.application.use_cases_async.fx_audit import (
    AsyncAddExchangeRateEvent,
    AsyncListExchangeRateEvents
)

# Добавление события курса
async with uow_factory() as uow:
    use_case = AsyncAddExchangeRateEvent(uow, clock)
    await use_case(
        currency_code="USD",
        rate=Decimal("75.50"),
        occurred_at=datetime.now(UTC),
        source="manual"
    )
```
```

### Для docs/RUNNING_MIGRATIONS.md

**Заменить** (строка 88):
```bash
poetry run python -m presentation.cli.main trading detailed --base USD
```

**На**:
```python
from py_accountant.application.use_cases_async.trading_balance import AsyncGetTradingBalance

async with uow_factory() as uow:
    use_case = AsyncGetTradingBalance(uow, clock)
    result = await use_case(base_currency="USD")
```

### Для docs/TRADING_WINDOWS.md

**Заменить** (строки 56-60):
```bash
poetry run python -m presentation.cli.main trading raw --json
```

**На**:
```python
from py_accountant.application.use_cases_async.trading_balance import AsyncGetTradingRaw

async with uow_factory() as uow:
    use_case = AsyncGetTradingRaw(uow, clock)
    result = await use_case(account_names=["Assets:Cash", "Liabilities:Cards"])
```

### Для docs/ARCHITECTURE_OVERVIEW.md

**Заменить** (строка 134):
```markdown
(Planned) CLI/Presentation: `src/presentation/cli/main.py`, `src/presentation/cli/*.py` — 
этот слой описан в архитектуре как возможное будущее HTTP/CLI API и может отсутствовать 
в текущей версии репозитория; интеграторы работают через core-пакет `py_accountant`.
```

**На**:
```markdown
Примечание: Presentation/CLI layer был удалён в версии 1.0.0 в пользу core-only архитектуры. 
Интеграторы работают напрямую через `py_accountant.application` (use cases и порты).
```

---

## Выводы

1. **Хорошая новость**: Большинство удалённых компонентов (ApplicationService, sync репозитории, старые импорты) уже корректно удалены из документации

2. **Критическая проблема**: 6 упоминаний presentation layer блокируют пользователей

3. **Объём работы**: 4 файла требуют правки (FX_AUDIT.md, RUNNING_MIGRATIONS.md, TRADING_WINDOWS.md, ARCHITECTURE_OVERVIEW.md)

4. **Оценка времени**: ~3-4 часа на исправление всех 6 упоминаний

---

## История изменений

| Дата | Версия | Изменения |
|------|--------|-----------|
| 2025-11-25 | 1.0 | Первичный аудит удалённых компонентов (Спринт S1) |

---

**Следующий шаг**: `docs/AUDIT_CODE_MAPPING.md` — матрица соответствия документация↔код

