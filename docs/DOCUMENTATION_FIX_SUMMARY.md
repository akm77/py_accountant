# Краткая сводка: Исправление документации py_accountant

**Дата:** 24 ноября 2025  
**Статус:** 📋 Готово к обсуждению

---

## 🎯 Суть проблемы

Документация py_accountant устарела после перехода на core-only архитектуру (v1.0.0):
- **Было:** Время интеграции ~30 минут
- **Сейчас:** Время интеграции ~4 часа (нужно изучать исходный код)
- **Причина:** Документация описывает API, который не существует

---

## 📊 Статистика проблем

| Категория | Количество | Примеры |
|-----------|-----------|---------|
| 🔴 Критические | 5 | Неправильные импорты (`account` vs `accounts`), несуществующие параметры API |
| 🟡 Средние | 3 | Отсутствие информации о том, какие use cases требуют `clock` |
| 🟢 Минорные | 2 | Устаревшие примеры с SDK |

---

## ✅ Предлагаемое решение: 3 фазы

### Фаза 1: Критические исправления (1-2 дня) ⚡

**Must have** — без этого новые интеграторы будут страдать

1. **Создать `docs/API_REFERENCE.md`** (4 часа)
   - Все use cases с правильными сигнатурами
   - Таблица "какие use cases требуют clock"
   - Структура всех DTOs с примерами
   - Иерархия исключений

2. **Исправить `docs/INTEGRATION_GUIDE.md`** (2 часа)
   - Заменить `account` → `accounts` в импортах
   - Добавить полный end-to-end пример
   - Показать реальные параметры `AsyncCreateAccount`

3. **Исправить `README.md`** (1 час)
   - Удалить устаревшие примеры SDK
   - Добавить работающий Quick Start

4. **Создать `docs/ERROR_HANDLING.md`** (2 часа)
   - Таблица исключений по каждому use case
   - Маппинг на HTTP коды
   - Примеры обработки

**Результат:** Время интеграции снизится с 4 часов до 30 минут ✅

---

### Фаза 2: Автоматизация (2-3 дня) 🤖

**Should have** — защита от будущих проблем

1. **Тесты для примеров документации** (4 часа)
   - `tests/docs/test_documentation_examples.py`
   - Все примеры из документации запускаются как тесты
   - CI ломается, если документация устаревает

2. **Настроить mkdocs** (4 часа)
   - Автогенерация API docs из docstrings
   - Single source of truth (код = документация)
   - Деплой на GitHub Pages

3. **Создать MIGRATION_GUIDE.md** (2 часа)
   - Таблица "было-стало" для миграции с v0.x
   - Скрипт автоматизации

**Результат:** Документация поддерживает себя сама ✅

---

### Фаза 3: Улучшения (опционально) 💎

**Nice to have** — делаем документацию best-in-class

1. Type stubs для IDE (6 часов)
2. Jupyter notebooks с примерами (8 часов)
3. Видео-туториалы (12 часов)

**Результат:** py_accountant — эталон документирования ✅

---

## 🎯 Ключевые исправления

### 1. Импорты

```diff
# ❌ Неправильно (в текущей документации)
- from py_accountant.application.use_cases_async.account import AsyncCreateAccount

# ✅ Правильно
+ from py_accountant.application.use_cases_async.accounts import AsyncCreateAccount
```

### 2. AsyncCreateAccount API

```diff
# ❌ Неправильно (в текущей документации)
- await AsyncCreateAccount(uow, clock)(
-     full_name="Asset:Bank:USD",
-     account_type=AccountType.ASSET,
-     currency=Currency("USD")
- )

# ✅ Правильно
+ await AsyncCreateAccount(uow)(  # ⚠️ clock удален!
+     full_name="Asset:Bank:USD",
+     currency_code="USD"  # ✅ Просто строка
+ )
```

### 3. EntryLineDTO структура

```diff
# ❌ Неправильно (в текущей документации)
- EntryLineDTO(
-     account="Asset:Bank:USD",
-     debit="100.00",
-     credit="0",
-     currency="USD"
- )

# ✅ Правильно
+ EntryLineDTO(
+     side="DEBIT",
+     account_full_name="Asset:Bank:USD",
+     amount=Decimal("100.00"),
+     currency_code="USD"
+ )
```

### 4. AsyncGetAccountBalance возвращаемое значение

```diff
# ❌ Неправильно (в текущей документации)
- balance_dto = await AsyncGetAccountBalance(uow, clock)(...)
- print(balance_dto.balance)

# ✅ Правильно
+ balance = await AsyncGetAccountBalance(uow, clock)(...)
+ print(balance)  # Decimal напрямую
```

---

## 📈 Метрики успеха

| Метрика | До | После | Улучшение |
|---------|-----|-------|-----------|
| Время первой интеграции | 4 часа | 30 минут | **8x** |
| Coverage примеров тестами | 0% | 100% | CI защита |
| Время обновления документации | 2 часа | 5 минут | Автогенерация |

---

## 🚀 План действий

### Сейчас (обсуждение)
1. ✅ Изучить предложение в `DOCUMENTATION_FIX_PROPOSAL.md`
2. ✅ Обсудить приоритеты и объемы
3. ✅ Скорректировать план при необходимости

### После утверждения
1. Создать issues для задач Фазы 1
2. Начать с `docs/API_REFERENCE.md` (самая критичная задача)
3. Итеративно исправлять документацию с проверкой тестами

---

## 📚 Документы

- **Полное предложение:** `docs/DOCUMENTATION_FIX_PROPOSAL.md` (детальный план на 11 страниц)
- **Этот документ:** Краткая сводка для быстрого ознакомления

---

## 💬 Вопросы для обсуждения

1. **Согласны ли с приоритизацией фаз?**
   - Фаза 1 (must) → Фаза 2 (should) → Фаза 3 (nice)

2. **Какие задачи хотите добавить/убрать?**

3. **Когда можем начать Фазу 1?**
   - Предложение: Сразу после утверждения плана

4. **Нужна ли Фаза 3 или достаточно Фаз 1-2?**

---

**Следующий шаг:** Обсудить и утвердить план → Создать issues → Начать реализацию 🚀

