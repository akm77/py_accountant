# Обновление TRADING_WINDOWS.md — Миграция с CLI на Python API

**Дата**: 2025-11-25  
**Тип изменения**: Критическое обновление устаревшей документации  
**Статус**: ✅ Завершено

## Проблема

Документ `docs/TRADING_WINDOWS.md` был полностью устаревшим:
- ❌ Описывал несуществующий CLI
- ❌ Содержал инструкции с флагами `--start`, `--end`, `--meta`, `--base`, `--json`
- ❌ Упоминал "SDK" который также удалён
- ❌ Не соответствовал текущей архитектуре (async-only, use cases)

## Что было изменено

### 1. Заголовок и введение
**Было**:
```markdown
Коротко: отчеты по операциям за окно времени. Две команды:
- trading raw — агрегирует по валютам без конверсии.
- trading detailed — агрегирует и конвертирует в базовую валюту.

CLI — async-only: команды вызывают асинхронные use case'ы
```

**Стало**:
```markdown
Коротко: отчеты по операциям за окно времени. Два use case:
- **AsyncGetTradingBalanceRaw** — агрегирует по валютам без конверсии.
- **AsyncGetTradingBalanceDetailed** — агрегирует и конвертирует в базовую валюту.

Оба use case являются async-only и работают через AsyncUnitOfWork.
```

### 2. Параметры
**Было** (CLI флаги):
```markdown
- Параметры: --start, --end — строки в ISO8601
- Флаг: --meta k=v (можно указывать несколько раз)
```

**Стало** (Python параметры):
```markdown
- Параметры: start, end — объекты datetime
- Параметр: meta: dict[str, str] | None — словарь для фильтрации
```

### 3. Секции команд
**Было**:
```markdown
## Команда: trading raw
Параметры:
- --start, --end — ISO8601
- --meta k=v — фильтр
- --json — вывод JSON
```

**Стало**:
```markdown
## Use Case: AsyncGetTradingBalanceRaw

**Импорт**:
from py_accountant.application.use_cases_async.trading_balance import AsyncGetTradingBalanceRaw

**Сигнатура**:
async def __call__(
    self,
    *,
    start: datetime | None = None,
    end: datetime | None = None,
    meta: dict[str, Any] | None = None,
) -> list[TradingBalanceLineSimple]

**Параметры**: [подробное описание]
**Возвращает**: [структура DTO]
**Примеры**: [рабочий Python код]
```

### 4. Примеры кода
**Было** (устаревший SDK):
```python
# Неполный импорт
from application.use_cases_async.trading_balance import AsyncGetTradingBalanceDetailed

# Странный вызов через app
async with app.uow_factory() as uow:
    detailed = await AsyncGetTradingBalanceDetailed(uow, app.clock)(...)
```

**Стало** (актуальный Python API):
```python
from py_accountant.application.use_cases_async.trading_balance import AsyncGetTradingBalanceRaw
from datetime import datetime, UTC

async with uow_factory() as uow:
    use_case = AsyncGetTradingBalanceRaw(uow, clock)
    
    result = await use_case(
        start=datetime(2025, 11, 10, 10, 0, 0, tzinfo=UTC),
        end=datetime(2025, 11, 10, 12, 0, 0, tzinfo=UTC),
        meta={"source": "exchange", "user": "alice"}
    )
```

### 5. Документация ошибок
**Было** (CLI-oriented):
```markdown
- Нет «=» → «Invalid meta item; expected k=v»
- Пустой --base → «Empty base currency code»
```

**Стало** (Python exceptions):
```markdown
### Meta фильтры
- Невалидный тип meta → TypeError

### Base currency
- ValueError("Empty base currency code") — пустая строка в base_currency
- ValueError("Base currency is not defined") — не указана и не найдена
```

### 6. Добавлены новые секции
- **Сигнатуры use cases** с полными type hints
- **Описание возвращаемых DTO** с полями и типами
- **Примеры для JSON сериализации** в каждой секции
- **Раздел "См. также"** со ссылками на связанную документацию

### 7. Удалено
- ❌ Все упоминания CLI команд
- ❌ Все флаги (--start, --end, --meta, --base, --json)
- ❌ Упоминания SDK
- ❌ Инструкции по формату вывода CLI
- ❌ Дублирующие примеры JSON

## Структура обновлённого документа

```
# Trading Windows (Async)
├── Введение (use cases вместо команд)
├── Назначение
├── Тайм-окно (datetime объекты вместо ISO8601 строк)
├── Квантизация и форматы
├── Фильтры meta (dict вместо флагов)
├── Use Case: AsyncGetTradingBalanceRaw
│   ├── Импорт
│   ├── Сигнатура
│   ├── Параметры
│   ├── Возвращает (DTO)
│   ├── Примеры (Python)
│   └── Формат для JSON
├── Use Case: AsyncGetTradingBalanceDetailed
│   ├── Импорт
│   ├── Сигнатура
│   ├── Параметры
│   ├── Определение базовой валюты
│   ├── Поведение конверсии
│   ├── Возвращает (DTO)
│   ├── Примеры (Python)
│   └── Формат для JSON
├── Ошибки и валидация (Python exceptions)
├── Ограничения и рекомендации
└── См. также (ссылки на другие документы)
```

## Метрики изменений

- **Строк изменено**: ~90% документа переписано
- **CLI упоминаний удалено**: 15+
- **Примеров кода добавлено**: 4 полноценных Python примера
- **Сигнатур документировано**: 2 (полные type hints)
- **DTO описано**: 2 (TradingBalanceLineSimple, TradingBalanceLineDetailed)
- **JSON примеров**: 2 (встроены в секции use cases)

## Проверка

### Тесты документации
```bash
poetry run pytest tests/docs/ -q
# Result: 18 passed in 0.33s ✅
```

### Валидация синтаксиса
- ✅ Все блоки Python кода синтаксически корректны
- ✅ Все импорты валидны
- ✅ Нет broken links
- ✅ Нет legacy references (CLI, SDK)

### Ручная проверка
- ✅ Примеры соответствуют реальному API
- ✅ Сигнатуры соответствуют коду в src/
- ✅ DTO поля соответствуют domain models
- ✅ Ошибки соответствуют фактическим exception messages

## Влияние

### Положительное
✅ Документ теперь актуален и полезен  
✅ Разработчики могут использовать примеры "как есть"  
✅ Нет путаницы с несуществующим CLI  
✅ Полная информация о Python API  
✅ Явные type hints помогают IDE  

### Риски
⚠️ Пользователи старой документации могут быть дезориентированы  
💡 **Решение**: В начале документа можно добавить примечание о миграции

## Рекомендации для будущего

1. **При удалении компонентов** (CLI, SDK) — сразу обновлять ВСЮ связанную документацию
2. **Автоматизировать проверку** — тесты документации предотвращают такие проблемы
3. **Code review** — проверять не только код, но и документацию
4. **Changelog** — отмечать breaking changes в документации

## См. также

- [SPRINT_S6_COMPLETED.md](SPRINT_S6_COMPLETED.md) — создание тестов документации
- [TEST_FIXES_SUMMARY.md](TEST_FIXES_SUMMARY.md) — исправление падающих тестов
- [API_REFERENCE.md](API_REFERENCE.md) — полная документация use cases
- [INTEGRATION_GUIDE.md](INTEGRATION_GUIDE.md) — примеры интеграции

---

**Автор**: GitHub Copilot  
**Дата**: 2025-11-25  
**Статус**: ✅ Завершено и проверено тестами

