# Repository Planning Graph (RPG) - Краткая методология

## 🎯 Что такое RPG?

**Repository Planning Graph (RPG)** — методология управления крупными проектами через **явные графы зависимостей** вместо расплывчатых текстовых описаний.

## 🏗️ Пять принципов RPG

### 1. Явная структура
**Всегда начинай с построения графа**

Вместо текстового описания "Сначала сделаем A, потом B" создаётся граф:
```
A → B → C
↓   ↓
D → E
```

Граф показывает:
- **Узлы** = компоненты (модули, классы, функции)
- **Рёбра** = зависимости и порядок реализации

### 2. Топологический порядок
**Реализуй зависимости раньше зависящих**

Порядок реализации определяется топологической сортировкой графа:
1. Сначала узлы без входящих рёбер (независимые)
2. Потом узлы, зависящие только от реализованных
3. И так далее, пока не будут реализованы все узлы

**Пример:**
```
Order: A → D → B → E → C
```

### 3. Стабильные интерфейсы
**Фиксируй входы/выходы до реализации**

Для каждого узла определяется контракт:
```python
class MyService:
    """Brief description."""
    
    def process(self, input: InputType) -> OutputType:
        """What it does, params, returns."""
        pass  # Реализация потом
```

Интерфейс замораживается → зависимые компоненты могут разрабатываться параллельно.

### 4. Модульность
**Группируй связанное, разделяй несвязанное**

- **Высокая связность** (cohesion) внутри модуля
- **Низкая связность** (coupling) между модулями
- Каждый модуль = отдельный узел в графе

**Пример структуры:**
```
domain/         # Бизнес-логика
application/    # Use cases
infrastructure/ # I/O, внешние зависимости
```

### 5. Инкрементальная валидация
**Тести каждый компонент перед интеграцией**

После реализации каждого узла:
1. Unit тесты узла
2. Integration тесты с зависимостями
3. Только после прохождения → интеграция

Это предотвращает накопление ошибок.

## 📊 Структура RPG графа

### Узлы (двойная семантика)

**Функциональный уровень:**
- Корневые узлы = модули высокого уровня
- Промежуточные = компоненты
- Листовые = конкретные функции/классы

**Структурный уровень:**
- Корневые узлы = директории
- Промежуточные = файлы
- Листовые = классы/функции в файлах

### Рёбра (зависимости)

- **Межмодульные** = потоки данных между модулями
- **Внутримодульные** = порядок файлов внутри модуля

## 🔧 Применение в py_accountant

### Граф проекта

Файл `py_accountant_rpg.yaml` содержит:

```yaml
rpg:
  metadata:        # Название, версия, описание
  metrics:         # Количество файлов, классов, функций
  structure:       # Полная иерархия (project → dirs → files → classes → methods)
  dependencies:    # Граф импортов между модулями
  functional_modules:  # Высокоуровневое представление
```

### Пример использования

**До разработки нового класса:**
1. Найди его место в графе (`structure`)
2. Проверь зависимости (`dependencies`)
3. Определи интерфейс (type hints + docstrings)
4. Реализуй класс
5. Тести
6. Обнови граф: `python -m tools.rpg`

**При рефакторинге:**
1. Проанализируй граф зависимостей
2. Определи затронутые компоненты
3. Обнови интерфейсы (если нужно)
4. Обнови зависимые компоненты
5. Тести
6. Обнови граф

## 🎯 Ключевые правила

### ✅ DO:
- Проектируй граф перед кодом
- Фиксируй интерфейсы первыми
- Реализуй в топологическом порядке
- Тести каждый компонент
- Обновляй граф после изменений

### ❌ DON'T:
- Не начинай кодить без графа
- Не меняй интерфейсы без причины
- Не создавай циклические зависимости
- Не интегрируй без тестов
- Не игнорируй граф при рефакторинге

## 📝 Чеклист для новой фичи

- [ ] **1. Спроектировать граф**
  - Какие компоненты нужны?
  - Какие зависимости между ними?
  - В каком порядке реализовывать?

- [ ] **2. Зафиксировать интерфейсы**
  - Публичные методы
  - Type hints
  - Docstrings

- [ ] **3. Реализовать в порядке**
  - От независимых к зависимым
  - По одному компоненту
  - С тестами после каждого

- [ ] **4. Интегрировать**
  - Unit тесты пройдены
  - Integration тесты пройдены
  - Граф обновлён

- [ ] **5. Валидация**
  - E2E тесты
  - Код ревью
  - Проверка графа на циклы

## 🔍 Анализ существующего графа

### Как читать py_accountant_rpg.yaml

**1. Метрики (общая оценка):**
```yaml
metrics:
  total_files: 52      # Размер проекта
  total_classes: 112   # Количество компонентов
  total_methods: 217   # Детализация
```

**2. Структура (иерархия):**
```yaml
structure:
  name: py-accountant
  children:
    - name: domain     # Модуль
      children:
        - name: ledger.py    # Файл
          children:
            - name: Ledger   # Класс
              method_count: 15
```

**3. Зависимости (граф импортов):**
```yaml
dependencies:
  "src/py_accountant/domain/ledger.py":
    - "py_accountant.domain.accounts"
    - "py_accountant.domain.currencies"
```

**4. Функциональные модули (архитектура):**
```yaml
functional_modules:
  - name: domain
    classes: ["Account", "Ledger", ...]
    functions: [...]
```

## 💡 Практические советы

### Для добавления класса:
1. `grep -A 20 "целевой_модуль" py_accountant_rpg.yaml`
2. Посмотри существующие классы в модуле
3. Проверь их зависимости
4. Следуй тому же паттерну

### Для рефакторинга:
1. Найди класс в RPG
2. Посмотри `dependencies` (кто зависит от него)
3. Если breaking change → обнови все зависимости
4. Обнови тесты
5. Обнови граф

### Для code review:
1. Проверь, что новый код в правильном модуле
2. Проверь, что зависимости логичны (нет циклов)
3. Проверь, что интерфейс стабилен
4. Проверь, что есть тесты
5. Проверь, что граф обновлён

## 🚀 Быстрый старт

**Для LLM-ассистента:**
1. Прочитай `py_accountant_rpg.yaml` для понимания структуры
2. При разработке нового класса → найди его место в графе
3. При изменении существующего → проверь зависимости
4. Всегда обновляй граф после изменений: `python -m tools.rpg`

**Для разработчика:**
1. Установи: `python -m tools.rpg` для генерации графа
2. Изучи граф перед началом работы
3. Планируй изменения с учётом зависимостей
4. Обновляй граф регулярно

## 📚 Дополнительно

- **Полная методология:** `docs/rpg_intro.txt`
- **Примеры промптов:** `tools/rpg/PROMPT_SNIPPETS.md`
- **Сравнение режимов:** `tools/rpg/MODES_COMPARISON.md`
- **Полная документация:** `tools/rpg/README.md`

---

## ⚡ TL;DR (самое главное)

**RPG = явный граф вместо текстового описания**

**5 принципов:**
1. Граф → структура
2. Топология → порядок
3. Интерфейсы → контракты
4. Модули → изоляция
5. Тесты → валидация

**Файл графа:** `py_accountant_rpg.yaml`  
**Генератор:** `python -m tools.rpg`  
**Использование:** Читай граф → планируй → реализуй → тести → обновляй граф

**Главное правило:** Всегда знай, где ты в графе! 🎯

