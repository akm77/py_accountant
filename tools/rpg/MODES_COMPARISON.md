# Сравнение режимов генерации RPG

## 📊 Статистика для проекта py_accountant

### Размеры файлов

| Режим | Флаги | Размер | Строк | Символов | Слов | Примерно токенов |
|-------|-------|--------|-------|----------|------|------------------|
| **Полный** (старый) | — | 138 KB | 3,624 | 141,521 | 9,335 | **~35,000-40,000** |
| **Базовый** (новый по умолчанию) | — | 97 KB | 2,864 | 99,012 | 5,443 | **~25,000-28,000** |
| **Компактный** | `--nodetail` | 57 KB | 1,617 | 58,400 | 3,066 | **~15,000-17,000** |
| **Полный с docstrings** | `--docstring` | 138 KB | 3,624 | 141,521 | 9,335 | **~35,000-40,000** |

### Сокращение размера

| Сравнение | Уменьшение строк | Уменьшение размера | Уменьшение токенов |
|-----------|------------------|--------------------|--------------------|
| Базовый vs Полный | **-21%** (760 строк) | **-30%** (42 KB) | **~-30%** (~10K токенов) |
| Компактный vs Полный | **-55%** (2,007 строк) | **-59%** (83 KB) | **~-60%** (~20K токенов) |
| Компактный vs Базовый | **-44%** (1,247 строк) | **-42%** (41 KB) | **~-45%** (~10K токенов) |

## 🎯 Режимы генерации

### 1. Полный режим (для документации)
```bash
python -m tools.rpg --docstring
```

**Включает:**
- ✅ Все классы с методами
- ✅ Docstrings модулей, классов, функций
- ✅ Детальные метаданные

**Использование:**
- Полная документация проекта
- Архивирование структуры
- Детальный анализ

**Размер:** ~138 KB, ~40K токенов  
**Подходит для:** Gemini 1.5 Pro (1M), Claude 3.x (200K), GPT-4-turbo (128K)

### 2. Базовый режим (по умолчанию)
```bash
python -m tools.rpg
```

**Включает:**
- ✅ Все классы с методами
- ❌ Без docstrings (для экономии места)
- ✅ Основные метаданные

**Использование:**
- Стандартная генерация
- Баланс детализации и размера
- Повседневное использование

**Размер:** ~97 KB, ~28K токенов  
**Подходит для:** Gemini 1.5 Pro (1M), Claude 3.x (200K), GPT-4-turbo (128K)

### 3. Компактный режим (для LLM с ограниченным контекстом)
```bash
python -m tools.rpg --nodetail
```

**Включает:**
- ✅ Классы с названиями и метаданными
- ❌ Без методов (только method_count)
- ❌ Без docstrings
- ✅ Функции верхнего уровня

**Использование:**
- Работа с моделями с ограниченным контекстом
- Быстрый обзор структуры
- Планирование архитектуры

**Размер:** ~57 KB, ~17K токенов  
**Подходит для:** GPT-4-32K (32K), Claude 3.x (200K), GPT-4-turbo (128K)

### 4. Минимальный режим (самый компактный)
```bash
python -m tools.rpg --nodetail --exclude tests --exclude examples
```

**Дополнительно исключает:**
- ❌ Тесты
- ❌ Примеры
- ❌ Другие указанные директории

**Использование:**
- Анализ только основного кода
- Максимальная экономия токенов

**Размер:** ~30-40 KB, ~8K-12K токенов  
**Подходит для:** Почти все модели, включая GPT-4 (8K) и GPT-3.5-turbo (16K)

## 🔍 Что включено в каждый режим

### Всегда присутствует:

```yaml
rpg:
  metadata:           # Название, версия, описание
  metrics:            # Количество файлов, классов, функций, строк
  structure:          # Иерархия проекта
    - directories     # Все директории
    - files           # Все файлы
    - classes         # Все классы
      - name          # Название класса
      - line_number   # Номер строки
      - decorators    # Декораторы
      - base_classes  # Базовые классы
      - method_count  # Количество методов
  dependencies:       # Граф импортов
  functional_modules: # Высокоуровневое представление
```

### Опционально (--docstring):

```yaml
  - docstring         # Для модулей, классов, функций
```

### Опционально (по умолчанию, без --nodetail):

```yaml
  classes:
    - methods:        # Список методов
      - name          # Название метода
      - line_number   # Номер строки
      - is_async      # Async/sync
      - decorators    # Декораторы
```

## 💡 Рекомендации по выбору режима

### Для документации проекта
```bash
python -m tools.rpg --docstring --output docs/project_structure.yaml
```

### Для работы с LLM
```bash
# GPT-4-turbo/Claude 3.x (базовый режим достаточен)
python -m tools.rpg

# GPT-4-32K (компактный режим)
python -m tools.rpg --nodetail

# GPT-4/GPT-3.5 (минимальный режим)
python -m tools.rpg --nodetail --exclude tests --exclude examples
```

### Для анализа архитектуры
```bash
# Только основные модули без деталей
python -m tools.rpg --nodetail --output architecture_overview.yaml
```

### Для CI/CD
```bash
# Базовый режим для отслеживания изменений
python -m tools.rpg --output project_structure.yaml
```

## 📈 Влияние на модели LLM

| Модель | Лимит контекста | Полный (40K) | Базовый (28K) | Компактный (17K) |
|--------|-----------------|--------------|---------------|------------------|
| GPT-3.5-turbo | 16K | ❌ 250% | ❌ 175% | ✅ 106% |
| GPT-4 | 8K | ❌ 500% | ❌ 350% | ❌ 212% |
| GPT-4-32K | 32K | ❌ 125% | ✅ 87% | ✅ 53% |
| GPT-4-turbo | 128K | ✅ 31% | ✅ 22% | ✅ 13% |
| Claude 3.x | 200K | ✅ 20% | ✅ 14% | ✅ 8% |
| Gemini 1.5 Pro | 1M | ✅ 4% | ✅ 3% | ✅ 2% |

**Легенда:**
- ✅ Влезет комфортно (< 80% контекста)
- ⚠️ Влезет впритык (80-100% контекста)
- ❌ Не влезет (> 100% контекста)

## 🎨 Примеры использования

### Пример 1: Стандартная генерация
```bash
python -m tools.rpg
# Создаёт py_accountant_rpg.yaml (97 KB, ~28K токенов)
```

### Пример 2: Для анализа в GPT-4-32K
```bash
python -m tools.rpg --nodetail --output rpg_compact.yaml
# Создаёт rpg_compact.yaml (57 KB, ~17K токенов)
```

### Пример 3: Полная документация
```bash
python -m tools.rpg --docstring --output rpg_full_docs.yaml
# Создаёт rpg_full_docs.yaml (138 KB, ~40K токенов)
```

### Пример 4: Только основной код
```bash
python -m tools.rpg --nodetail --exclude tests --exclude examples --exclude docs
# Создаёт py_accountant_rpg.yaml (~30-40 KB, ~10K токенов)
```

### Пример 5: Анализ конкретного модуля
```bash
python -m tools.rpg --src-dir src/py_accountant/domain --nodetail
# Анализирует только domain модуль в компактном режиме
```

## 🔧 Флаги командной строки

| Флаг | Значение по умолчанию | Описание |
|------|----------------------|----------|
| `--docstring` | `False` | Включить docstrings (увеличивает размер на ~40-50%) |
| `--nodetail` | `False` | Исключить методы из классов (уменьшает размер на ~40-50%) |
| `--exclude PATTERN` | стандартные | Исключить директории/файлы по паттерну |
| `--src-dir PATH` | `src` | Директория для анализа |
| `--output PATH` | `<name>_rpg.yaml` | Файл вывода |
| `--verbose` | `False` | Подробный вывод |

## 📝 Итоговые рекомендации

### ✅ По умолчанию (базовый режим)
```bash
python -m tools.rpg
```
- Без docstrings (экономия ~30%)
- С методами (полная структура)
- **Оптимально для большинства случаев**

### ✅ Для работы с LLM с ограниченным контекстом
```bash
python -m tools.rpg --nodetail
```
- Без docstrings
- Без методов (только количество)
- **Уменьшение на ~60% от полной версии**

### ✅ Для полной документации (редко)
```bash
python -m tools.rpg --docstring
```
- С docstrings
- С методами
- **Используется для архивных целей**

---

**Вывод:** Новые флаги позволяют гибко управлять размером RPG файла, уменьшая его от 138 KB до 57 KB (и даже меньше с --exclude), что делает его пригодным для использования с широким спектром LLM моделей.

