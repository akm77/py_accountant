# RPG Methodology Prompt Snippets

Коллекция сниппетов для включения методологии RPG в промпты при разработке.

---

## Snippet #1: Краткое введение в RPG (для быстрой вставки)

```markdown
## 📊 Repository Planning Graph (RPG) Methodology

Этот проект следует методологии RPG для управления сложной архитектурой:

**Ключевые принципы:**
1. **Явная структура** - всегда начинай с построения графа зависимостей
2. **Топологический порядок** - реализуй зависимости раньше зависящих компонентов
3. **Стабильные интерфейсы** - фиксируй входы/выходы до реализации
4. **Модульность** - группируй связанное, разделяй несвязанное
5. **Инкрементальная валидация** - тести каждый компонент перед интеграцией

**Граф проекта:** См. `py_accountant_rpg.yaml` для текущей структуры проекта.

**Требование:** При добавлении новых компонентов всегда учитывай их место в графе зависимостей.
```

---

## Snippet #2: Для разработки нового класса

```markdown
## 🎯 Задача: Разработка нового класса

### Контекст проекта (RPG Methodology)

**Текущая структура проекта:**
<attach: py_accountant_rpg.yaml>

**Методология RPG требует:**

1. **Определи позицию в графе:**
   - В какой модуль входит класс? (domain/application/infrastructure)
   - От каких компонентов зависит? (проверь dependencies в RPG)
   - Какие компоненты будут зависеть от него?

2. **Зафиксируй интерфейс первым:**
   - Определи публичные методы
   - Определи типы параметров и возвращаемых значений
   - Напиши docstrings для всех публичных методов

3. **Проверь Clean Architecture:**
   - Domain: только бизнес-логика, без I/O
   - Application: use cases, координация domain объектов
   - Infrastructure: I/O, внешние зависимости, реализация портов

4. **Следуй паттернам проекта:**
   - Используй dataclasses для value objects
   - Используй Protocol/ABC для интерфейсов
   - Избегай циклических зависимостей

### Требования к реализации:

- [ ] Класс размещён в правильном модуле согласно Clean Architecture
- [ ] Все зависимости явно объявлены (type hints)
- [ ] Интерфейс задокументирован (docstrings)
- [ ] Написаны unit тесты
- [ ] Обновлён RPG граф (если добавлены новые зависимости)

### Инструкции:

1. Проанализируй `py_accountant_rpg.yaml` для понимания структуры
2. Определи правильное место для нового класса
3. Спроектируй интерфейс с учётом существующих паттернов
4. Реализуй класс
5. Напиши тесты
6. Обнови документацию

**Примечание:** Проект использует Python 3.13, asyncio, SQLAlchemy 2.0, pydantic 2.x
```

---

## Snippet #3: Для расширения функционала существующего класса

```markdown
## 🔧 Задача: Расширение функционала существующего класса

### Контекст проекта (RPG Methodology)

**Текущая структура проекта:**
<attach: py_accountant_rpg.yaml>

**Целевой класс:** `[ИМЯ_КЛАССА]`
**Модуль:** `[ПУТЬ_К_МОДУЛЮ]`

### Анализ по RPG методологии:

1. **Изучи текущее положение в графе:**
   ```bash
   # Найди класс в RPG графе
   grep -A 20 "[ИМЯ_КЛАССА]" py_accountant_rpg.yaml
   ```
   
   - Какие компоненты зависят от этого класса?
   - От каких компонентов зависит сам класс?
   - Сколько методов уже есть? (method_count)

2. **Проверь стабильность интерфейса:**
   - Можно ли добавить функционал БЕЗ изменения существующих методов?
   - Если нужно изменить существующие методы - это breaking change?
   - Какие компоненты потребуют обновления?

3. **Оцени влияние изменений:**
   - Используй граф зависимостей из RPG для определения затронутых компонентов
   - Проверь тесты зависимых компонентов

### Требования к расширению:

- [ ] Новый функционал не нарушает существующий интерфейс (backward compatible)
- [ ] Все новые методы задокументированы (docstrings)
- [ ] Обновлены существующие тесты (если нужно)
- [ ] Написаны тесты для нового функционала
- [ ] Обновлена документация (если публичный API изменился)
- [ ] Проверена совместимость с зависимыми компонентами

### Чеклист изменений:

1. **Перед изменениями:**
   - [ ] Прочитай существующий код класса
   - [ ] Изучи существующие тесты
   - [ ] Проверь зависимости в RPG графе
   - [ ] Определи, нужны ли breaking changes

2. **Во время изменений:**
   - [ ] Сохраняй стиль кода проекта
   - [ ] Добавляй type hints
   - [ ] Пиши docstrings
   - [ ] Логируй важные операции (structlog)

3. **После изменений:**
   - [ ] Запусти тесты: `pytest tests/unit/[модуль]`
   - [ ] Проверь покрытие: `pytest --cov`
   - [ ] Обнови RPG граф: `python -m tools.rpg`
   - [ ] Проверь линтер: `ruff check`

**Примечание:** При breaking changes обязательно обнови все зависимые компоненты.

---

## Snippet #4: Полный контекст проекта (для сложных задач)

```
## 📦 Контекст проекта py_accountant

### Методология: Repository Planning Graph (RPG)

Проект использует RPG для управления архитектурой через явные графы зависимостей.

**Принципы RPG:**
1. Явная структура → граф зависимостей (`py_accountant_rpg.yaml`)
2. Топологический порядок → реализация от независимых к зависимым
3. Стабильные интерфейсы → публичный API фиксируется до реализации
4. Модульность → Clean Architecture (domain/application/infrastructure)
5. Инкрементальная валидация → тесты для каждого компонента

### Структура проекта (из RPG графа):

py_accountant/
├── domain/              # Бизнес-логика (без I/O)
│   ├── accounts.py      # Account, AccountName
│   ├── ledger.py        # Ledger (главный агрегат)
│   ├── currencies.py    # Currency, ExchangeRate
│   ├── value_objects.py # Value objects
│   └── errors.py        # Domain exceptions
├── application/         # Use cases
│   ├── use_cases/
│   │   ├── ledger.py    # Ledger operations
│   │   ├── exchange_rates.py
│   │   └── recalculate.py
│   ├── ports.py         # Interfaces (Repository, UnitOfWork)
│   └── dto/             # Data Transfer Objects
└── infrastructure/      # I/O, внешние зависимости
    ├── persistence/     # Реализации репозиториев
    │   ├── sqlalchemy/  # SQLAlchemy repositories
    │   └── inmemory/    # In-memory repositories (для тестов)
    ├── migrations/      # Alembic + Migration API
    └── config/          # Конфигурация
```

### Метрики проекта:
- 📁 52 Python файла
- 📝 5,926 строк кода
- 🏛️ 112 классов
- ⚙️ 46 функций
- 🔧 217 методов

### Ключевые паттерны:

**1. Clean Architecture:**
- Domain: чистая бизнес-логика, immutable где возможно
- Application: use cases принимают UnitOfWork
- Infrastructure: реализует порты из application

**2. Dependency Injection:**
```python
# Use case принимает UnitOfWork
async def some_use_case(uow: UnitOfWork) -> Result:
    async with uow:
        # Работа с репозиториями через uow
        account = await uow.accounts.get(...)
        await uow.commit()
```

**3. Repository Pattern:**
```python
class AccountRepository(Protocol):
    async def add(self, account: Account) -> None: ...
    async def get(self, full_name: str) -> Account | None: ...
```

**4. Unit of Work Pattern:**
```python
async with uow:
    # Atomic операции
    await uow.accounts.add(account)
    await uow.journals.add(journal)
    await uow.commit()  # All or nothing
```

### Технологический стек:
- Python 3.13
- SQLAlchemy 2.0 (async)
- asyncpg / aiosqlite
- pydantic 2.x (settings, validation)
- Alembic (migrations)
- structlog (logging)
- pytest + pytest-asyncio (testing)

### Граф зависимостей:

**Важные зависимости (из RPG):**
- `domain.ledger` ← зависит от: `domain.accounts`, `domain.currencies`
- `application.use_cases.ledger` ← зависит от: `domain.ledger`, `application.ports`
- `infrastructure.persistence.sqlalchemy` ← зависит от: `domain.*`, `application.ports`

### Требования к новому коду:

1. **Архитектура:**
   - ✅ Следуй Clean Architecture
   - ✅ Domain код НЕ должен зависеть от infrastructure
   - ✅ Используй ports для абстракций

2. **Код:**
   - ✅ Type hints везде (Python 3.13 style)
   - ✅ Docstrings для публичных методов
   - ✅ Async/await где нужны I/O операции
   - ✅ Immutable объекты где возможно (dataclass + frozen=True)

3. **Тесты:**
   - ✅ Unit тесты для domain логики
   - ✅ Integration тесты для repositories
   - ✅ E2E тесты для use cases
   - ✅ Используй fixtures из conftest.py

4. **Стиль:**
   - ✅ Следуй существующим паттернам проекта
   - ✅ Используй structlog для логирования
   - ✅ Обрабатывай ошибки через domain exceptions
   - ✅ Проходи ruff check (линтер)

### RPG граф проекта:

См. прикреплённый файл `py_accountant_rpg.yaml` для полной структуры.

**Как использовать RPG граф:**
1. Найди свой модуль в секции `structure`
2. Проверь `dependencies` для понимания зависимостей
3. Изучи `functional_modules` для общей архитектуры
4. При добавлении новых зависимостей - обнови граф: `python -m tools.rpg`

---

### Инструкции для LLM:

1. **Всегда начинай с анализа RPG графа** - найди целевой модуль/класс
2. **Проверяй зависимости** - убедись, что не создаёшь циклы
3. **Следуй топологическому порядку** - реализуй зависимости первыми
4. **Сохраняй стабильные интерфейсы** - backward compatibility важна
5. **Тести инкрементально** - после каждого компонента

**Готов к работе!** Теперь можешь выполнять задачу с учётом контекста проекта.

---

## Snippet #5: Мини-версия (для простых задач)

```markdown
### RPG Context

**Проект:** py_accountant (async accounting ledger, Clean Architecture)
**RPG граф:** `py_accountant_rpg.yaml` (52 файла, 112 классов)

**Структура:**
- `domain/` - бизнес-логика
- `application/` - use cases + ports
- `infrastructure/` - I/O + repositories

**Правила:**
1. Domain не зависит от infrastructure
2. Use cases принимают UnitOfWork
3. Type hints + docstrings обязательны
4. Async/await для I/O операций

**Тесты:** `pytest tests/unit/` или `tests/integration/`
**Обнови RPG:** `python -m tools.rpg` после изменений
```

---

## Snippet #6: Для анализа существующего кода

```markdown
## 🔍 Задача: Анализ существующего кода

### Контекст (RPG Methodology)

**RPG граф проекта:** `py_accountant_rpg.yaml`

### Инструкции по анализу:

1. **Найди компонент в RPG графе:**
   ```yaml
   # Структура в RPG:
   structure:
     - name: [MODULE]
       type: directory/file
       children: [...]
   ```

2. **Проверь зависимости:**
   ```yaml
   dependencies:
     "path/to/file.py": ["dep1", "dep2"]
   ```

3. **Оцени метрики:**
   - Количество методов (method_count)
   - Количество строк (line_count)
   - Декораторы (decorators)
   - Базовые классы (base_classes)

4. **Анализируй по принципам RPG:**
   - Стабильность интерфейса (сколько компонентов зависит от него?)
   - Связность (все ли методы относятся к одной ответственности?)
   - Coupling (насколько сильно связан с другими компонентами?)

5. **Предложения по улучшению:**
   - Можно ли уменьшить зависимости?
   - Нужно ли разделить на несколько классов?
   - Соблюдается ли Clean Architecture?

**Результат:** Отчёт с рекомендациями по рефакторингу (если нужно).

---

## Snippet #7: Для code review

```markdown
## 📝 Code Review с учётом RPG

### Чеклист:

**Архитектура (RPG principles):**
- [ ] Компонент размещён в правильном слое (domain/application/infrastructure)
- [ ] Зависимости соответствуют топологическому порядку (нет циклов)
- [ ] Интерфейсы стабильны (не нарушают существующий API без необходимости)
- [ ] Модульность: класс имеет одну ответственность

**Код:**
- [ ] Type hints везде
- [ ] Docstrings для публичных методов
- [ ] Обработка ошибок (domain exceptions)
- [ ] Логирование (structlog) в критичных местах

**Тесты:**
- [ ] Unit тесты написаны
- [ ] Покрытие достаточное (>80%)
- [ ] Тесты проходят: `pytest`

**RPG граф:**
- [ ] Граф обновлён: `python -m tools.rpg`
- [ ] Новые зависимости задокументированы

**Совместимость:**
- [ ] Backward compatible (или breaking change задокументирован)
- [ ] Зависимые компоненты обновлены (если нужно)
```

---

## Как использовать эти сниппеты

### В начале промпта:
1. Выбери подходящий сниппет (1-7)
2. Вставь его в начало промпта
3. Приложи файл `py_accountant_rpg.yaml` (если нужен полный контекст)
4. Опиши конкретную задачу

### Пример промпта:

```markdown
[ВСТАВЬ SNIPPET 2 - для нового класса]

## Конкретная задача:

Создай новый класс `TradingWindowService` в модуле `domain/services/`.

**Требования:**
- Методы: `is_trading_active()`, `get_window_config()`
- Зависимости: `Clock` (infrastructure), `TradingWindowConfig` (domain)
- Без I/O операций (domain layer)

**Контекст:** Нужно для проверки, активно ли торговое окно для пересчёта балансов.
```

---

## Результат использования RPG

Теперь модель будет:
1. ✅ Понимать методологию RPG
2. ✅ Учитывать структуру проекта из графа
3. ✅ Следовать Clean Architecture
4. ✅ Проверять зависимости
5. ✅ Обновлять RPG граф после изменений

**Файлы:**
- Сниппеты: `tools/rpg/PROMPT_SNIPPETS.md`
- RPG граф: `py_accountant_rpg.yaml`
- Методология: `docs/rpg_intro.txt`

